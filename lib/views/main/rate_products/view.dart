import 'package:flutter/material.dart';

class RateProductView extends StatelessWidget {
  const RateProductView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
